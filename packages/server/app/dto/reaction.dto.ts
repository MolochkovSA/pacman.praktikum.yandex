export interface ReactionDto {
  commentId: number;
  reactionId: number;
  author: string;
  createdAt: string;
}
export interface EmojiDto {
  id: number;
  emoji: string;
  createdAt: string;
  updatedAt: string;
}
